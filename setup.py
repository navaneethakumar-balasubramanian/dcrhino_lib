from setuptools import setup

setup(name='dcrhino',
      version='0.1',
      description='Libraries to work with rhino data',
      url='https://github.com/DatacloudIntl/dcrhino_lib',
      author='Karl',
      packages=['dcrhino'],
      zip_safe=False)
